## 1. Project Scaffold

- [x] 1.1 Create `package.json` with extension manifest (name `reltio-metadata-editor`, publisher, engines `^1.85.0`, activationEvents `onLanguage:json`, main `./dist/extension.js`, contributes.jsonValidation for `*.reltio.json`)
- [x] 1.2 Create `tsconfig.json` (target ES2022, module Node16, strict, outDir `dist/`, rootDir `src/`)
- [x] 1.3 Create `src/extension.ts` with empty `activate()` and `deactivate()` functions
- [x] 1.4 Install devDependencies: `typescript`, `@types/vscode`, `@types/node`, `esbuild`
- [x] 1.5 Install dependencies: `jsonc-parser`
- [x] 1.6 Create esbuild.config.mjs with `mainFields: ["module", "main"]` and `external: ["vscode"]`

## 2. TypeScript Code Model — Root and Top-Level Types

- [x] 2.1 Create `src/model/types.ts` with `ReltioBusinessModel` interface (17 child arrays/objects + 12 metadata properties: schemaVersion, createdTime, updatedTime, createdBy, updatedBy, referenceConfigurationURI, abstract, flatOvCalculation, label, uri, rdmTenantId, pinIgnoredPriority)
- [x] 2.2 Add `EntityType` interface (~30 child elements: description, attributes[], matchGroups[], survivorshipGroups[], associationGroups[], surrogateCrosswalks[], cleanseConfig, cleanse[], dataPipelineConfig, activenessAttributes, geoLocationAttributes, dataLabelPattern, secondaryLabelPattern, dataTooltipPattern, entityTypeRoleURIs, businessCardAttributeURIs, imageAttributeURIs, matchAssets, defaultFacetedAttributes, defaultSearchAttributes, dependentAttributes[], lifecycleActions, analyticsAttributes, extendsTypeURI, hidden[], unmerge, ruleBasedAttributes[], indexingConfig, groupLabels; ~22 attributes: uri, label, typeIcon, typeImage, typeGraphIcon, typeColor, entitySmartLogic, abstract, singleDefaultCrosswalk, matchBeforeCreate, configurationLessMatching, cleanseInputObjectBeforeOverwrite, searchable, validateRequiredAttributes, overrideIgnorePin, uniqueGeneratedValuePerEntityPerRequest, ignoreWarnings, skipValidationForAbstractType, ignoreNonOVChangesWhenUpdateThroughReferencedEntity, useOnlyOvValuesInReferencedEntities, forceOvForUiUpdates, defaultSurvivorshipStrategy)
- [x] 2.3 Add `RelationType` interface (child elements: description, attributes[], startObject, endObject, activenessAttributes, survivorshipGroups[], cleanse[], cleanseConfig, dataPipelineConfig, analyticsAttributes, indexingConfig, extendsTypeURI, lifecycleActions, precedingCleanseProcedureUris, succeedingCleanseProcedureUris; attributes: uri, label, direction, implicit, typeColor, reverseOfTypeURIs, sameAsTypeURIs, singleDefaultCrosswalk, validateRequiredAttributes, entityEndDateStrategy, ignoreWarnings, contributorCrosswalkSingleSourceUpdateMode, defaultSurvivorshipStrategy)
- [x] 2.4 Add `RelationEndpoint` interface (for startObject/endObject: objectTypeURI, uri, directionalContext[]) and `DirectionalContext` interface (labelPattern, uri, rule) and `Rule` interface (value, condition, type, attribute)
- [x] 2.5 Add remaining top-level types: `AttributeTypeDefinition` (uri, valueType, extendsTypeURI, matchTokenClass, comparatorClass), `ChangeRequestType` (uri, lifecycleActions), `Role` (uri, label, description, extendsTypeURI), `GroupType` (type, label, uri + children), `InteractionType` (label, uri + children), `GraphType` (uri, label + children), `CategoryType` (uri, label + children), `HierarchyType` (uri, label, allowedEntityTypes[]), `SurvivorshipStrategy` (uri, label, parentStrategyURI, mainSourceType, winnerSourceType, winnerSourceAttributes, primaryAttributeUri, default), `Source` (uri, label, icon, abbreviation, priority, autoGenerated, generator, autoGenerationPattern, description, indexingConfig), `Rating` (sources, userRoles, timestamp), `Activity` (uri, attributes[]), `GroupingType` (uri, label, source, entityType, groupForSingleEntity, description, members[], attributeMappings[])
- [x] 2.6 Add `MatchAction` interface (name, mapping[]) and `MatchActionMapping` (class, parameters[])

## 3. TypeScript Code Model — Attribute System

- [x] 3.1 Add `Attribute` interface with all ~60 properties from XSD: uri (required), type, name, label, hidden, important, faceted, searchable, system, required, prioritizeOv, matchOvOnly, matchFieldURIsIgnoreCase, keyAttributeURIsIgnoreCase, lookupCode2, resolveLookupCode, survivorshipStrategy, dataLabelPattern, relationshipLabelPattern, relationshipTypeURI, referencedEntityTypeURI, referenceAttributeDirection, autoGenerated, generator, autoGenerationPattern, generateIfEmpty, generateIfNotEmpty, generatedValueUniqueForCrosswalk, defaultValue, skipInDataAccess, customReltioId, vectorized; child elements: values, dependentLookupAttributes[], attributes[] (recursive), referencedAttributeURIs[], immutableForSources[], immutableExceptForSources[], immutable, doNotOverrideForSourceURIs[], matchFieldURIs, matchFieldURIsExactOrNull, matchFieldURIsExactOrAllNull, keyAttributeURIs, keyAttributeURIsExactOrNull, keyAttributeURIsExactOrAllNull, singleValue, singleValueByCrosswalk, singleValueByCrosswalkSources, description, attributeOrdering, masking, autoGenerationNestedPattern, cardinality, defaultValue (element), indexingConfig, enableNestedPartialOverride, lookupCode
- [x] 3.2 Add `InteractionAttribute` interface (subset of Attribute for interaction types)
- [x] 3.3 Add `AnalyticsAttribute` interface (uri, type, name, label, maxOccurs, couldUpdateWithLCA, dataLabelPattern, faceted, searchable, description, analyticsAttributes recursive)
- [x] 3.4 Add `ActivityAttribute` interface (uri, type, name, label, searchable, description)
- [x] 3.5 Add supporting attribute types: `Cardinality` (minValue, maxValue), `AttributeMasking` (regexPattern, maskMatched), `AttributeOrdering` (orderType, fieldURI, orderingStrategy), `SingleValueByCrosswalkSources` (include[], exclude[]), `DataPipelineConfig` (enabled, attributes[]), `ActivenessAttributes` (type, uri), `GeoAttributes` (latitude, longitude)

## 4. TypeScript Code Model — Match System

- [x] 4.1 Add `MatchGroup` interface (type, scope, label, uri, useOvOnly, scoreStandalone, scoreIncremental, matchMethod, matchServiceClass, crossTypeMatch; children: matchMethodMetadata, rule, negativeRule, documentComparator, documentTokenizer, aliasing, overrides, methods, postEvaluation, candidatesLookup, matchPrerequisites)
- [x] 4.2 Add `MatchRule` interface (recursive: exact[], fuzzy[], exactOrNull[], exactOrAllNull[], ignoreInToken[], or[], and[], not[], cleanse, notExact[], equals[], notEquals[], in[], multi[], matchTokenClasses, comparatorClasses, nullValues[], weights, actionThresholds; attributes: matchTokenClass, comparatorClass)
- [x] 4.3 Add `NegativeMatchRule` interface (notExactSame[], notFuzzySame[], equals[], notEquals[], in[], nullValues[], or[], and[], comparatorClasses; attributes: matchTokenClass, comparatorClass)
- [x] 4.4 Add match sub-types: `MatchAssets` (algorithm, rematchOnThreshold, assetDefinitions[]), `AssetDefinition` (uri, label, pattern, threshold, description, attributes[], sourceSystem[]), `AssociationGroup` (uri, label, groupEntityTypes, targetType, targetUri, scoreStandalone, scoreIncremental, rule)
- [x] 4.5 Add match mapping types: `MatchTokenClassMapping`, `ComparatorClassMapping` (attribute, class, parameters[]), `MatchAttributeParameter` (parameter, value, values), `DocumentComparatorMapping` (class, parameters[], fernConfig), `DocumentTokenizerMapping`
- [x] 4.6 Add match support types: `MatchMethodMetadata` (packageVersion, packageId, packageProfile, origin, matchMethodApplicability), `MatchGroupAliasing` + `AliasMapping`, `MatchGroupOverrides` + `AttributeMapping`, `CompositeMatchMethods` + `MatchMethodLink` + `WeightingAlgorithm`, `MatchPostEvaluation`, `CandidatesLookup` + `LookupMethod`, `MatchPrerequisites` + `PrerequisiteRule` + `PrerequisiteFilter`
- [x] 4.7 Add match operand types: `EqualsOperand` (uri, value, values, strict, weight, checkNulls), `NotExactOperand` (uri, multiValueAnyMismatchIsPositive), `MultiOperand` (uri, attributes[], combinationsConfig), `CombinationsConfig` (threshold, strategy), `AttributeWeight` (attribute, weight), `ActionThreshold` (type, threshold, label), `NullValues` (value, attribute), `MatchRuleCleanse` (cleanseAdapter, cleanseAdapterParams, mappings[], attributes[])
- [x] 4.8 Add `FernConfig`, `FeatureDefaults`, `FeatureOverrides`, `FeatureOverrideEntry`, `FeatureOverride`, `Prefilter` interfaces

## 5. TypeScript Code Model — Survivorship System

- [x] 5.1 Add `SurvivorshipGroup` interface (uri, label, default, roles[], sourcesUriOrder[], sourcesForOv[], mapping, description)
- [x] 5.2 Add `SurvivorshipMapping` interface (recursive via fallbackStrategies[]: survivorshipStrategy, attribute, sortAs, comparisonAttributeUri, comparisonAttributeOvOnly, primaryAttributeUri, fallbackUsingCriteria, ignoreCase; children: sourcesUriOrder[], valuesPriorityOrder[], lookupComparisonField[], sourcesForOv[], filter, fallbackStrategiesChain[], fallbackStrategies[])
- [x] 5.3 Add `SurvivorshipFilter` interface (equals[], ne[], lt[], lte[], gt[], gte[], missing[], exists[], in[], notIn[], or[], and[], discardIgnoredDeletedValues) and all criteria types: `FilterEqualsCriteria`, `FilterNotEqualsCriteria`, `FilterLessCriteria`, `FilterLessOrEqualCriteria`, `FilterGreatCriteria`, `FilterGreatOrEqualCriteria`, `FilterMissingCriteria`, `FilterExistsCriteria`, `FilterInCriteria`, `FilterNotInCriteria`, `OrCondition`, `AndCondition`

## 6. TypeScript Code Model — Cleanse System

- [x] 6.1 Add `CleanseInfo` interface (cleanseProvider, uri, resultingValuesSourceTypeUri, inputMapping[], outputMapping[])
- [x] 6.2 Add `CleanseConfig` interface (uri, mappings[], infos[], addressAutoCompleteConfig, attributeVerificationConfig[])
- [x] 6.3 Add cleanse sub-types: `CleanseMappings` (uri, inputMapping/inputMappingRef, outputMapping/outputMappingRef), `CleanseInfos` (uri, useInCleansing, nestedAttributeToCleanse, sequence[]), `CleanseSequence` (uri, chain[]), `CleanseChain` (uri, cleanseFunction, resultingValuesSourceTypeUri, proceedOnSuccess, proceedOnFailure, removeOldCleanseResultsOnFailure, mapping, params)
- [x] 6.4 Add `CleanseMappingDefinition` (attribute, cleanseAttribute, uri, mandatory, allValues)
- [x] 6.5 Add `AddressAutoCompleteConfig` (uri, minSearchTextLen, providerOpts, inputMapping/inputMappingRef, outputMapping/outputMappingRef) and `AttributeVerificationConfig` (uri, resultingValuesSourceTypeUri, verificationFunction, params, inputMapping[], outputMapping[]) and `VerificationMappingDefinition` (attribute, verificationAttribute, mandatory, uri)

## 7. TypeScript Code Model — Remaining Types

- [x] 7.1 Add `SurrogateCrosswalk` interface (source, generationLogic, enforce, attributes[])
- [x] 7.2 Add `DependentAttributes` (attributeUri, default[], values[]) and `DependentAttributeValues` (valuesList[], visibleAttributes[])
- [x] 7.3 Add `RuleBasedAttribute` (name, type, uri, label, description, controlFunction) and `ControlFunction` (expression, showAttributeURI[])
- [x] 7.4 Add `EntityUnmerge` (enabled, retainManualMerges, retainMergeConditions, actions[]), `RetainMergeCondition` (sources[], matchGroups[]), `UnmergeMatchGroup` (uri), `UnmergeAction` (enabled, proceed, type, parameters[])
- [x] 7.5 Add `HiddenEntityTypeConfiguration` (matchGroups[], skippedMatchRules[])
- [x] 7.6 Add `IndexingConfig` (enabled, predicateEnabled, segmentationEnabled, consistency, caseSensitive, subAttributeValuesLimit, attributes[])
- [x] 7.7 Add `GroupingMember` (entityType, relationType, groupingRule), `GroupingAttributeMapping` (uri, function), `GroupingFunction` (type, config, survivorshipGroupMapping), `GroupElement` (uri, entityTypeURIs, reachByRelationTypeURIs), `MemberType` (uri, label, name, primaryMember, minOccurs, objectTypeURI)
- [x] 7.8 Add generic map/entry types: `MapEntry` (key, value) as a simple key-value pair for XSD `map`/`hashMap` types

## 8. TypeScript Code Model — Enum / Union Types

- [x] 8.1 Add all enum/union types:
  - `MatchGroupScope`: `"none" | "internal" | "external" | "all" | "ml_match" | "NONE" | "INTERNAL" | "EXTERNAL" | "ALL" | "ML_MATCH"`
  - `MatchMethod`: `"match_iq" | "rule_based"`
  - `EntityEndDateStrategy`: `"END_DATE_RELATION" | "END_DATE_RELATION_CROSSWALKS"`
  - `FallbackUsingCriteria`: `"ZERO_OR_MORE_THAN_ONE" | "MORE_THAN_ONE" | "ZERO"`
  - `GenerationLogic`: `"ovOnly" | "useFirstNonOvWhenOvMissing" | "generateUidWhenOvMissing" | "generateUidWhenOvAndNonOvMissing" | "generateUidWhenAllOvMissing"`
  - `GroupingFunctionType`: `"fixedValue" | "copy" | "survivedValue" | "aggregate" | "count" | "countDistinct" | "groupingRule" | "srcCopy" | "topK" | "mostRecent" | "firstNonNull"`
  - `RuleBasedAttributeType`: `"Conditional"`
  - `PromptTemplate`: `"SINGLEVALUE" | "MULTIVALUE"`
  - `MultiAttributeCombinationsStrategy`: `"DROP_ALL" | "FIRST_N" | "NOOP"`

## 9. JSON Schema

- [x] 9.1 Create `schemas/reltio-metadata.schema.json` with `$schema: "http://json-schema.org/draft-07/schema#"`, top-level properties mirroring `ReltioBusinessModel`, and `$defs` for every interface
- [x] 9.2 Add `description` fields on all properties for VS Code hover documentation
- [x] 9.3 Set `additionalProperties: false` on all object definitions
- [x] 9.4 Register schema via `contributes.jsonValidation` in `package.json` for `*.reltio.json`
- [x] 9.5 Allow `$schema` property at top level for inline schema references

## 10. Verification

- [x] 10.1 Create `samples/example.reltio.json` with representative entity types, relation types, attributes, match groups, survivorship, and sources
- [x] 10.2 Verify the sample validates against the JSON Schema without errors
- [x] 10.3 Verify unknown properties produce validation errors
